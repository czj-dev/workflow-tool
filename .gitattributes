# 默认所有文本文件用 LF，避免 .sh / 源码在 Windows 检出为 CRLF 后跨平台出错
* text=auto eol=lf

# Windows 专用脚本保留 CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.icns binary
